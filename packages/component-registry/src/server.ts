/**
 * Server-side only exports for component registry
 * Use this for server-side operations that require Node.js APIs
 */

// Metadata loader (server-side only)
export { MetadataLoader, DEFAULT_METADATA_PATHS } from './loaders/json-loader';