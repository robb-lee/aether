{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @aether/web build",
  "devCommand": "pnpm --filter @aether/web dev",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet .",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  }
}